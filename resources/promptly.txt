Let me find the exact scan area button layout first.

Good, now I have the full picture. Here are the exact steps:

Step 1 — Add the Rescan button definition in the scan area (after self.trend_btn definition, before btn_row.addWidget calls, around line 3547):

Add this block:

        self.rescan_btn = QPushButton("🔄  Rescan")
        self.rescan_btn.setCursor(Qt.CursorShape.PointingHandCursor)
        self.rescan_btn.setFixedSize(150, 44)
        self.rescan_btn.setToolTip("Re-run a fresh scan using the software list from the currently loaded CSV")
        self.rescan_btn.setStyleSheet(ACTION_BTN_STYLE.replace(C.RED, C.YELLOW))


Step 2 — Add it to btn_row (after self.trend_btn is added, around line 3556):

        btn_row.addSpacing(5)
        btn_row.addWidget(self.rescan_btn)


Step 3 — Connect its signal (with the other .clicked.connect calls, around line 3561):

        self.rescan_btn.clicked.connect(self._rescan_from_loaded_csv)


Step 4 — Remove the Rescan button from the Results Browser source bar (around line 3756). Remove these lines:

        rescan_btn = QPushButton("🔄  Rescan")
        rescan_btn.setFixedHeight(28)
        rescan_btn.setToolTip(
            "Re-run a fresh scan using the software list from the currently loaded CSV"
        )
        rescan_btn.setStyleSheet(
            f"QPushButton {{ background:{C.BG_INPUT}; color:{C.FG}; "
            f"border:1px solid {C.BORDER}; border-radius:4px; "
            f"padding:2px 10px; font-size:11px; }}"
            f"QPushButton:hover {{ background:{C.BG_HOVER}; }}"
            f"QPushButton:disabled {{ background:{C.BORDER}; color:{C.FG_DIM}; }}"
        )
        rescan_btn.clicked.connect(self._rescan_from_loaded_csv)
        self._rescan_btn = rescan_btn


And remove its widget addition:

        source_bar.addWidget(rescan_btn)


Step 5 — Update self._rescan_btn — since the button is now self.rescan_btn (a proper instance attribute like scan_btn, stop_btn etc.) you can remove the self._rescan_btn = rescan_btn line entirely from step 4, as the reference is already stored as self.rescan_btn in step 1.

That’s all four files locations touched, all in draugr.py. Want me to implement it?