Looking at the screenshot, Start Scan, Stop Scan, and Compare Scans have icons that are blending into the button. Rescan, Skip Current, and Scan History look crisper. The difference is likely the icon characters themselves — let me check what’s being used.

That’s the issue. ▶, ■, and Δ are plain Unicode characters that render as text glyphs — thin and flat. The ones that pop (⏭, 📈, 🔄) are emoji/pictographs that render with visual weight. Simple fix — swap them for emoji equivalents:

Also need to update the two places where scan_btn text is reset after a scan:

All consistent. Copying to outputs:

Four icons swapped:

	•	▶ → ▶️ (Start Scan — updated in definition and both setText reset calls)
	•	■ → ⏹️ (Stop Scan)
	•	⏭ → ⏭️ (Skip Current — the variation selector added for consistency)
	•	Δ → 🔀 (Compare Scans — the Greek delta was the most visually weak, swapped for the shuffle/compare arrows emoji)