draugr.py — insert after the from urllib.parse import quote_plus line:

# ── Subdirectory path injection ────────────────────────────────────────
# Companion modules are organised into subdirectories. Adding them to
# sys.path here means all imports throughout the file stay unchanged.
_APP_DIR = Path(__file__).parent
for _subdir in ("core", "reports", "intelligence"):
    _p = _APP_DIR / _subdir
    if _p.exists() and str(_p) not in sys.path:
        sys.path.insert(0, str(_p))


draugr_reports.py — two things:

Add import sys to the standard library block (it’s missing there currently):

import sys


Then insert after the from typing import ... line:

# ── Subdirectory path injection ────────────────────────────────────────
_APP_DIR = Path(__file__).parent.parent   # reports/ → project root
for _subdir in ("core", "reports", "intelligence"):
    _p = _APP_DIR / _subdir
    if _p.exists() and str(_p) not in sys.path:
        sys.path.insert(0, str(_p))


The only difference between the two is .parent vs .parent.parent — draugr.py is at the root so one level up is enough, draugr_reports.py will be inside reports/ so it needs to climb two levels to reach the project root.